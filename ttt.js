var board = document.getElementById("board");
for(var i = 0; i < 9; i++) {
	var newDiv = document.createElement("div");
	board.appendChild(newDiv);
	newDiv.innerHTML = i;
	newDiv.addEventListener("mouseenter", function().style.backgroundColor="blue";
	newDiv.addEventListener("mouseleave").style.backgroundColor="blue";
}